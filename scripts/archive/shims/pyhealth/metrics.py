"""Stub metric fns — never called in --predict mode; present only to satisfy the import."""
def binary_metrics_fn(*a, **k): return {}
def multiclass_metrics_fn(*a, **k): return {}
def multilabel_metrics_fn(*a, **k): return {}
