# Reproducing the analysis

**Moved.** The canonical reproducibility guide is [`REPRODUCE.md`](../REPRODUCE.md) at the repo root
(BDSP release standard): the three `reproduce_story.sh` tiers, the data-access step
(`aws s3 sync` from the credentialed prefix), the **figure/table/number → script → input → output**
contract, and the key-numbers map. See also [`DATA_SOURCE.md`](../DATA_SOURCE.md) for provenance.
