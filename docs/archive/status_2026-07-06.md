# Project status, plan, and path to publication — 2026-07-06

## 1. What runs automatically tonight (you can sleep)
- **Cohort recompute fleet** (`fleet/scale_cohort*.sh`, tag `morgoth-cohort`): reprocessing ALL ~11,020
  cohort recordings (4,916 normal + 6,104 abnormal/rest) through the identical pipeline as the overnight
  expansion (extract.py features + Morgoth staging). Writes to S3 `Growth_curves/cohort_recompute/`.
- **Monitor** (`fleet/monitor_cohort.sh`, tracked): tears the fleet down at completion (~10,700 done) or a
  4-hour hard cap. Cost bounded to ~$150 worst case; on-demand.
- **Finish driver** (`fleet/finish_cohort_recompute.sh`, tracked): waits for the fleet to drain, then
  autonomously (each step logged to `/tmp/finish_cohort.log`, failures don't abort):
  1. pulls the recomputed cohort features,
  2. rebuilds the uniform table with BOTH cohorts on extract.py (`scripts/82_build_uniform_v2.py`),
  3. re-pulls OMOP fractional ages (`scripts/71`),
  4. regenerates growth curves (UNION), keystone (rel_delta/TAR/DAR, all features now comparable),
     topoplots, harmonization diagnostic, discrimination,
  5. **copies derived data + figures to the S3 data location** (`Growth_curves/derived/`, `figures/`),
  6. rebuilds the dashboard, commits.
  → In the morning: verify the outputs, then finalize the manuscript numbers.

## 2. What we accomplished this session (major)
- **Fleet + data:** completed the N3/overnight expansion (~16,055 recordings), giving dense deep-sleep
  coverage across the lifespan (N3 was the main gap). OMOP fractional ages (month-level in infancy),
  99.99% resolved. Found + fixed a fleet **cost-safety bug** (teardown matched the wrong tag/profile).
- **Normative model upgrades:** GAMLSS/LMS with **BCT + age-varying skewness** (fixes an infant-age median
  bias that was skewness, not smoothing); sliding age-widening rolling-median QC overlay; log-age axis.
- **Sex is dispensable — decision to POOL sexes.** On the real TAR/DAR detector, sex-conditional vs pooled
  norms differ by ΔAUROC ≤0.002 everywhere (`scripts/74`). Manuscript-ready.
- **TAR index bug fixed** (was theta/beta, now theta/alpha) in the cohort parser.
- **Cohort↔overnight difference — fully investigated, ruled out as artifact** (`results/pipeline_control.md`):
  identical channels; delta pipeline-consistent (rel_delta bias −0.016 on the same recordings); the
  difference is real (drowsy overnight wake + 100% disjoint inpatient population). Alpha-band ratio features
  (TAR/DAR) were NOT cross-comparable → motivated the recompute.
- **Union-as-normal justified** (`results/union_normal_detection.md`): using both report-normal cohorts as
  the standard costs no detection power (union AUROC ≈ single-cohort). Conservative, defensible.
- **Cohort recompute (running):** puts BOTH cohorts on the identical pipeline so ALL features (incl.
  TAR/DAR/alpha) are cross-comparable and the union is valid for everything — norm AND detection.
- **Figure 1 (keystone):** growth curves × top discriminating features (rel_delta/TAR/DAR) per stage.
- All committed on `main`; review docs in `results/overnight_review.md`, `results/pipeline_control.md`,
  `results/union_normal_detection.md`.

## 3. Manuscript status (`docs/manuscript_draft.md`)
The draft is thorough but **predates this session** — it describes the pre-expansion state (12,379
recordings, sex-specific curves, kernel-quantile fits, N3 gap "planned"). It must be updated once the
rerun numbers are final. Key changes to fold in:
- Cohort now ~12,000 routine + **~16,000 overnight** recordings; N3 gap **filled** (not "planned").
- Norms are **sex-pooled** (justified by ΔAUROC ≤0.002), not sex-specific.
- **GAMLSS/LMS BCT** growth-chart method (Cole-Green/Rigby-Stasinopoulos), not ad-hoc kernel quantiles.
- **Union-of-both-cohorts** normal; the routine-vs-overnight vigilance/population analysis + pipeline
  control as a Methods/Results subsection (a genuine methodological contribution).
- **Both cohorts recomputed by identical code** — remove the "MATLAB code unavailable / r=0.89–0.95
  validation" framing in favor of "single reproducible pipeline for all recordings."
- New **Figure 1 keystone**; OMOP fractional ages.
Numbers (AUROCs, Ns, discrimination) will be refreshed by the rerun before we rewrite the Results.

## 4. Remaining steps to publication
1. **[auto, tonight]** Finish recompute → rebuild → regenerate → S3 → dashboard.
2. **Verify** the rebuilt union curves + keystone (all features), coverage, calibration (AM).
3. **Refresh all numbers** on the fully-recomputed data: discrimination AUCs, detection, report agreement,
   age-AUROC, van Putten/BSI comparison — the full `reanalyze.sh` set (fix its stale paths first, same
   bug class as finalize.sh).
4. **Rewrite the manuscript** Results/Methods/Abstract with final numbers + the new architecture (§3).
5. **Detection against the union norm** using the recomputed abnormal cases (now pipeline-matched);
   consider the rEEG-vs-cEEG recording-type split.
6. **Figures:** finalize Figure 1 (keystone), F5 pipeline schematic (still TBD), regenerate all panels.
7. **External/generalizability** framing (single health system) — already flagged; cross-site train/test.
8. **Release:** de-identified label set + growth-curve tables + package; DOI/version; data-availability.
9. **Prospective validation** framing: blinded expert re-read of high-deviation report-normal sleep studies
   (the "normative model detects what reports miss" hypothesis) — the strongest novel claim.
