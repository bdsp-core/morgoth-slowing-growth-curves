# Core scientific stack
numpy
scipy
pandas
pyarrow            # parquet
pyyaml

# Signal / EEG
mne                # EEG IO + montage helpers (OccasionNoise EDF read)
pyedflib           # BIDS EDF read (io/edf.py load_edf_referential)
h5py               # MoE panel .mat (MATLAB v7.3 / HDF5)
# multitaper: scipy.signal.windows.dpss (no extra dep) or nitime/spectrum if preferred

# NB: torch + timm are DELIBERATELY NOT here. The Morgoth stager/gate runs in a SEPARATE venv (PILOT_VENV),
# set up from morgoth2/requirements.txt (timm 1.0.11, tensorboardX, …). This worker venv imports neither
# torch nor timm — the only torch importer is scripts/shims/eeg_level_wrap.py, which runs inside Morgoth's
# env as a subprocess. Two-venv design: see docs/fleet_dependencies.md §4 and docs/BOOTSTRAP.md §2.
# rclone is a BINARY (not pip): install separately + configure the s3/bdsp remotes (see docs/BOOTSTRAP.md)

# Modeling (normative growth curves)
scikit-learn
statsmodels        # quantile regression, GAM-ish
# gamlss equivalent in python: pygam (GAMs) — chosen model per config
pygam

# Data access
boto3              # S3 (library)
awscli             # REQUIRED: worker shells out to `aws s3 cp` for panel sources + output sync (not just boto3)
s3fs               # pandas/parquet directly from s3
psycopg[binary]    # OMOP (Postgres) — age/sex lookup

# Tables / reporting
tableone           # Table 1 cohort characteristics

# Viz
matplotlib
seaborn

# Dev / notebooks
jupyter
pytest
