# Core scientific stack
numpy
scipy
pandas
pyarrow            # parquet
pyyaml

# Signal / EEG
mne                # EEG IO + montage helpers (OccasionNoise EDF read)
pyedflib           # BIDS EDF read (io/edf.py load_edf_referential)
h5py               # MoE panel .mat (MATLAB v7.3 / HDF5)
# multitaper: scipy.signal.windows.dpss (no extra dep) or nitime/spectrum if preferred

# Morgoth foundation model (stager + gate). Needs KMP_DUPLICATE_LIB_OK=TRUE + PYTORCH_ENABLE_MPS_FALLBACK=1.
# HARD pin: timm==0.9.16 — the Morgoth model class expects that timm API (newer timm changes it).
# torch: the pilot ran on 2.13.0 (Python 3.14 / macOS-arm), but that version may not have wheels for your
# Python/platform (some environments cap at 2.8.0). Install the newest torch your env offers (>=2.1); the
# Morgoth path is not torch-version-fragile EXCEPT the EEG-level nested-tensor fast-path, which is already
# neutralized by scripts/shims/eeg_level_wrap.py. ALWAYS run the smoke test after install (BOOTSTRAP §"smoke").
torch>=2.1
timm==0.9.16
# rclone is a BINARY (not pip): install separately + configure the s3/bdsp remotes (see docs/BOOTSTRAP.md)

# Modeling (normative growth curves)
scikit-learn
statsmodels        # quantile regression, GAM-ish
# gamlss equivalent in python: pygam (GAMs) — chosen model per config
pygam

# Data access
boto3              # S3
s3fs               # pandas/parquet directly from s3
psycopg[binary]    # OMOP (Postgres) — age/sex lookup

# Tables / reporting
tableone           # Table 1 cohort characteristics

# Viz
matplotlib
seaborn

# Dev / notebooks
jupyter
pytest
